# Project Rules

## Dashboard Preview
After pushing changes that affect the dashboard, always share the preview link with the user so they can verify. Preview URL: http://159.223.127.125/preview/ (append ?v=<random> to bust cache).
