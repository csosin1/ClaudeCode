# zramswap config — see /etc/default/zramswap defaults file for full options
# Chosen for text-heavy idle Claude chat workload: LZ4 (fast), 50% of RAM, high priority.
ALGO=lz4
PERCENT=50
PRIORITY=100
